//
// Prefix header for all source files of the 'Ad Hoc Distribution Assistant' target in the 'Ad Hoc Distribution Assistant' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>

	// Remove NSLog on release builds
	#ifndef __OPTIMIZE__
	#define NSLog(...) NSLog(__VA_ARGS__)
	#else
	#define NSLog(...) {}
	#endif
#endif
